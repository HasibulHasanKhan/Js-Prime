/**
 * Create a new array with only elements that pass a conditiojn of a callback function.
 * use-case: When we need to create a new array that only contains elements meeting specific criteria .
 
Key Points:
 Returns a new array, leaving the original array unchanged.
 Only includes elements that satisfy the condition.
 Useful for selecting specific elements from an array.

 */
// "" = split letter with ,
// " " = split word with ,
//"an" = split from an  with ,

// const numbers = [1, 2, 3, 4, 5];

// const evenNumber = numbers.filter((value, index, arr) => {
//   return value % 2 === 0;
// });
// console.log(evenNumber);
